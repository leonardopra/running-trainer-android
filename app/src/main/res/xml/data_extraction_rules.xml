<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12+ (cloud backup + device-to-device transfer). Same rationale as
     backup_rules.xml: keep the API key off cloud backups and D2D transfers. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="file" path="datastore/" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="file" path="datastore/" />
    </device-transfer>
</data-extraction-rules>
